# 日志窗口滚动位置修复

## 问题描述

日志窗口打开时显示的是中间位置的日志，而不是最新的日志（底部）。

## 修复内容

### 1. 后端读取逻辑修改 (`src-tauri/src/logging.rs`)

#### 修改前
- 从文件开头开始读取
- `offset` 表示跳过前面多少条
- 读取顺序：旧 → 新

#### 修改后
- 从文件末尾开始读取
- `offset` 表示从末尾开始的偏移量（0 = 最新）
- 读取顺序：新 → 旧

```rust
/// 读取日志文件（分页，从末尾开始）
/// offset: 从末尾开始的偏移量（0 表示最新的日志）
/// limit: 要读取的日志数量
pub fn read_logs_from_file(file_path: &PathBuf, offset: usize, limit: usize) -> Result<Vec<LogMessage>, String> {
    // 读取所有行
    let all_lines: Vec<String> = reader.lines()
        .filter_map(|line| line.ok())
        .collect();
    
    let total = all_lines.len();
    
    // 计算从末尾开始的起始位置
    let start = if offset + limit > total {
        0
    } else {
        total - offset - limit
    };
    
    let end = total - offset;
    
    // 返回 [start..end] 范围的日志
    // ...
}
```

### 2. 前端加载逻辑修改 (`src/store/logStore.ts`)

#### loadLogs (初始加载)
- `offset = 0`: 从最新的日志开始
- `limit = 100`: 加载最新的 100 条
- 返回的日志按时间顺序排列（旧 → 新）

#### loadMoreLogs (加载更多)
- `offset = logs.length`: 当前已加载的数量
- `limit = 50`: 每次加载 50 条更早的日志
- 将新日志插入到列表**开头**（因为它们是更早的）

```typescript
loadMoreLogs: async () => {
  const offset = logs.length; // 已加载的数量
  const response = await invoke('get_logs', { offset, limit: 50 });
  
  set((state) => ({
    // 插入到开头
    logs: [...response.logs, ...state.logs],
    hasMore: response.has_more,
  }));
}
```

### 3. UI 滚动逻辑修改 (`src/components/LogView/LogWindow.tsx`)

#### 初始加载后自动滚动到底部
```typescript
loadLogs(0, 100).then(() => {
  setIsInitialLoad(false);
  // 加载完成后滚动到底部
  setTimeout(() => {
    if (logContainerRef.current) {
      logContainerRef.current.scrollTop = logContainerRef.current.scrollHeight;
    }
  }, 100);
});
```

#### 向上滚动加载更多时保持位置
```typescript
const handleScroll = (e) => {
  if (target.scrollTop < 100 && hasMore && !loading) {
    const scrollHeight = target.scrollHeight;
    const scrollTop = target.scrollTop;
    
    loadMoreLogs().then(() => {
      // 保持相对位置
      const newScrollHeight = logContainerRef.current.scrollHeight;
      const heightDiff = newScrollHeight - scrollHeight;
      logContainerRef.current.scrollTop = scrollTop + heightDiff;
    });
  }
};
```

## 工作流程

### 初始加载
```
1. 打开日志窗口
   ↓
2. loadLogs(0, 100)
   ↓
3. 后端读取最新 100 条日志
   ↓
4. 前端显示日志
   ↓
5. 自动滚动到底部 ✓
```

### 向上滚动加载更多
```
1. 用户向上滚动到顶部
   ↓
2. 触发 loadMoreLogs()
   ↓
3. offset = 100 (已加载的数量)
   ↓
4. 后端读取第 101-150 条（从末尾算）
   ↓
5. 插入到列表开头
   ↓
6. 保持滚动位置 ✓
```

### 实时日志追加
```
1. 后端发送新日志
   ↓
2. addLog() 追加到列表末尾
   ↓
3. 如果启用自动滚动，滚动到底部 ✓
```

## 示例场景

### 场景 1: 文件有 500 条日志

**初始加载**
- `loadLogs(0, 100)`
- 读取第 401-500 条（最新的 100 条）
- 显示在窗口，滚动到底部

**向上滚动**
- `loadMoreLogs()` with `offset=100`
- 读取第 301-400 条
- 插入到列表开头
- 保持滚动位置

**再次向上滚动**
- `loadMoreLogs()` with `offset=200`
- 读取第 201-300 条
- 插入到列表开头
- 保持滚动位置

### 场景 2: 文件只有 50 条日志

**初始加载**
- `loadLogs(0, 100)`
- 读取全部 50 条
- `hasMore = false`
- 显示在窗口，滚动到底部

**向上滚动**
- 不触发加载（hasMore = false）

## 对比效果

### 修复前
- ❌ 打开窗口显示中间位置
- ❌ 需要手动滚动到底部查看最新日志
- ❌ 向下滚动加载更多（不符合直觉）

### 修复后
- ✅ 打开窗口自动显示最新日志（底部）
- ✅ 向上滚动加载更早的历史日志
- ✅ 实时日志自动追加到底部
- ✅ 符合常见日志查看器的使用习惯

## 技术细节

### offset 的含义变化

**修复前**
- offset = 0: 第 1 条日志
- offset = 100: 第 101 条日志
- 方向：从旧到新 →

**修复后**
- offset = 0: 最新的日志
- offset = 100: 倒数第 101 条日志
- 方向：从新到旧 ←

### 滚动位置计算

加载更多日志后，需要调整滚动位置以保持用户当前查看的内容不变：

```typescript
// 记录加载前的状态
const scrollHeight = target.scrollHeight;  // 总高度
const scrollTop = target.scrollTop;        // 滚动位置

// 加载新日志后
const newScrollHeight = logContainerRef.current.scrollHeight;
const heightDiff = newScrollHeight - scrollHeight;  // 新增的高度

// 调整滚动位置
logContainerRef.current.scrollTop = scrollTop + heightDiff;
```

## 测试建议

1. **初始加载测试**
   - 打开日志窗口
   - 确认显示最新日志
   - 确认自动滚动到底部

2. **向上滚动测试**
   - 向上滚动到顶部
   - 确认加载更早的日志
   - 确认滚动位置保持不变

3. **实时日志测试**
   - 执行图生成新日志
   - 确认新日志追加到底部
   - 确认自动滚动（如果启用）

4. **边界测试**
   - 日志少于 100 条
   - 日志多于 1000 条
   - 快速连续滚动

## 相关文件

- `src-tauri/src/logging.rs` - 后端日志读取逻辑
- `src/store/logStore.ts` - 前端日志状态管理
- `src/components/LogView/LogWindow.tsx` - 日志窗口 UI

## 完成时间
2026-02-02
