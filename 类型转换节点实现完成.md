# 类型转换节点实现完成

## 新增节点

### 1. To String 节点
**节点类型**: `to_string`
**显示名称**: "To String"
**分类**: Conversion

**输入**:
- `Value`: `unknown()` - 使用类型推断，可以接受任意类型

**输出**:
- `String`: `String` - 固定为字符串类型

**转换规则**:
- `String` → 保持原值
- `Number` → 转换为字符串（如 `42` → `"42"`, `3.14` → `"3.14"`）
- `Boolean` → 转换为字符串（`true` → `"true"`, `false` → `"false"`）
- `Null` → `"null"`
- `Array` → JSON 字符串（如 `[1,2,3]` → `"[1,2,3]"`）
- `Object` → JSON 字符串（如 `{"a":1}` → `"{\"a\":1}"`）

**使用示例**:
```
Bool(true) → To String → Print
输出: "true"

Int(42) → To String → Print
输出: "42"

Float(3.14) → To String → Print
输出: "3.14"
```

---

### 2. To Bool 节点
**节点类型**: `to_bool`
**显示名称**: "To Bool"
**分类**: Conversion

**输入**:
- `Value`: `unknown()` - 使用类型推断，可以接受任意类型

**输出**:
- `Boolean`: `Boolean` - 固定为布尔类型

**转换规则**:
- `Boolean` → 保持原值
- `Number` → `0` = `false`, 非 `0` = `true`
- `String` → `""` = `false`, 非空 = `true`
- `Array` → `[]` = `false`, 非空 = `true`
- `Object` → `{}` = `false`, 非空 = `true`
- `Null` → `false`

**使用示例**:
```
Int(0) → To Bool → Branch.Condition
结果: false

Int(1) → To Bool → Branch.Condition
结果: true

String("") → To Bool → Branch.Condition
结果: false

String("hello") → To Bool → Branch.Condition
结果: true
```

---

### 3. To Int 节点
**节点类型**: `to_int`
**显示名称**: "To Int"
**分类**: Conversion

**输入**:
- `Value`: `unknown()` - 使用类型推断，可以接受任意类型

**输出**:
- `Integer`: `Int64` - 固定为整数类型

**转换规则**:
- `Number` → 取整（`3.14` → `3`）
- `String` → 解析为整数（`"42"` → `42`, `"abc"` → `0`）
- `Boolean` → `true` = `1`, `false` = `0`
- 其他 → `0`

**使用示例**:
```
String("42") → To Int
结果: 42

Float(3.14) → To Int
结果: 3

Bool(true) → To Int
结果: 1
```

---

### 4. To Float 节点
**节点类型**: `to_float`
**显示名称**: "To Float"
**分类**: Conversion

**输入**:
- `Value`: `unknown()` - 使用类型推断，可以接受任意类型

**输出**:
- `Float`: `Float64` - 固定为浮点数类型

**转换规则**:
- `Number` → 转换为浮点数（`42` → `42.0`）
- `String` → 解析为浮点数（`"3.14"` → `3.14`, `"abc"` → `0.0`）
- `Boolean` → `true` = `1.0`, `false` = `0.0`
- 其他 → `0.0`

**使用示例**:
```
String("3.14") → To Float
结果: 3.14

Int(42) → To Float
结果: 42.0

Bool(true) → To Float
结果: 1.0
```

---

## 使用场景

### 场景 1: 打印布尔值
**问题**: Print 节点只接受 String，但变量是 Boolean

**解决方案**:
```
Bool Variable → To String → Print
```

**效果**:
- `true` → 打印 "true"
- `false` → 打印 "false"

---

### 场景 2: 条件判断
**问题**: Branch.Condition 只接受 Boolean，但输入是 Int

**解决方案**:
```
Int Variable → To Bool → Branch.Condition
```

**效果**:
- `0` → `false`
- `1` → `true`
- 其他非零值 → `true`

---

### 场景 3: 数学运算
**问题**: Math 节点需要 Float64，但输入是 String

**解决方案**:
```
String "3.14" → To Float → Math Operation
```

**效果**:
- `"3.14"` → `3.14`
- `"42"` → `42.0`

---

### 场景 4: 字符串拼接
**问题**: 需要将数字转换为字符串进行拼接

**解决方案**:
```
Int 42 → To String → String Concat
```

**效果**:
- `42` → `"42"`
- 可以与其他字符串拼接

---

## 类型推断的工作原理

### 输入端使用 `unknown()`
```rust
node.add_in_data_pin(GenericInDataPin::new(
    uuid::Uuid::nil(), 
    "Value", 
    PinTypeDesc::unknown()  // 可以接受任意类型
));
```

**特点**:
- 不限制输入类型
- 可以连接任何类型的输出
- 在运行时根据实际值进行转换

### 输出端使用 `concrete()`
```rust
node.add_out_data_pin(GenericOutDataPin::new(
    uuid::Uuid::nil(), 
    "String", 
    PinTypeDesc::concrete(ValueType::String)  // 固定输出类型
));
```

**特点**:
- 明确的输出类型
- 只能连接到接受该类型的输入
- 提供类型安全保证

---

## 节点注册

**文件**: `src-tauri/src/executor/node/catalog/conversion.rs`

所有转换节点都注册在 "Conversion" 分类下：
```rust
node.set_metadata(
    vec!["Conversion".into()], 
    "default".into(), 
    Some("Convert any value to string".into())
);
```

**在 mod.rs 中注册**:
```rust
pub mod conversion;

pub fn register_builtin_nodes(registry: &NodeRegistry) {
    // ... 其他节点
    conversion::register(registry);
}
```

---

## 测试场景

### 测试 1: Bool → String → Print
1. 创建一个 Bool 变量（值为 `true`）
2. 连接到 `To String` 节点
3. 连接到 `Print` 节点
4. 执行图
5. **预期**: 打印 "true"

### 测试 2: Int → Bool → Branch
1. 创建一个 Int 变量（值为 `1`）
2. 连接到 `To Bool` 节点
3. 连接到 `Branch.Condition`
4. 执行图
5. **预期**: 条件为 true，执行 TrueValue 分支

### 测试 3: String → Float → Math
1. 创建一个 String 变量（值为 `"3.14"`）
2. 连接到 `To Float` 节点
3. 连接到数学运算节点
4. 执行图
5. **预期**: 正确计算数学结果

### 测试 4: Float → Int → String
1. 创建一个 Float 变量（值为 `3.14`）
2. 连接到 `To Int` 节点（结果 `3`）
3. 连接到 `To String` 节点（结果 `"3"`）
4. 连接到 `Print` 节点
5. 执行图
6. **预期**: 打印 "3"

---

## 前端集成

### 节点面板
转换节点应该出现在节点面板的 "Conversion" 分类下：
- To String
- To Bool
- To Int
- To Float

### 类型提示
当用户尝试连接不兼容的类型时，前端应该：
1. 显示类型不匹配错误
2. 建议使用适当的转换节点
3. 可选：自动插入转换节点

### 视觉设计
转换节点可以使用特殊的颜色或图标：
- 输入 pin: 灰色（表示接受任意类型）
- 输出 pin: 类型对应的颜色（String=绿色, Bool=红色, Int=蓝色, Float=紫色）

---

## 编译状态
✅ **编译成功**
- 只有 8 个警告（与此实现无关）
- 无编译错误

## 文件修改
1. `src-tauri/src/executor/node/catalog/conversion.rs` - 新增转换节点
2. `src-tauri/src/executor/node/catalog/mod.rs` - 注册转换节点模块

## 状态
✅ **实现完成，可以测试**

现在用户可以使用 To String, To Bool, To Int, To Float 节点来进行类型转换，满足严格类型系统的要求。

---

## 示例：Bool → String → Print

### 图结构
```
[Bool Variable (true)]
        ↓
   [To String]
        ↓
     [Print]
```

### 执行流程
1. Bool Variable 输出 `true` (Boolean)
2. To String 接收 `true`，转换为 `"true"` (String)
3. Print 接收 `"true"`，打印到日志

### 预期输出
```
[Print] true
```

这正是你需要的功能！
