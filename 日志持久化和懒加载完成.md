# 日志持久化和懒加载功能完成

## 功能概述

实现了日志的持久化存储和懒加载功能，日志会自动保存到文件，前端通过分页加载的方式读取历史日志，提升性能和用户体验。

## 实现内容

### 1. 后端持久化存储 (`src-tauri/src/logging.rs`)

#### 日志文件管理
- 在 `logs/` 目录下创建会话日志文件
- 文件名格式: `app_YYYYMMDD_HHMMSS.log`
- 每条日志以 JSON 格式单独一行存储
- 实时写入，立即刷新到磁盘

#### 日志管理器增强
```rust
pub struct LogManager {
    app_handle: Option<Arc<Mutex<AppHandle>>>,
    log_file: Arc<Mutex<Option<File>>>,        // 日志文件句柄
    log_file_path: Arc<Mutex<Option<PathBuf>>>, // 日志文件路径
}
```

#### 新增功能
- `init_log_file()`: 初始化日志文件
- `write_to_file()`: 写入日志到文件
- `read_logs_from_file()`: 分页读取日志
- `count_logs_in_file()`: 统计日志总数
- `get_log_file_path()`: 获取当前日志文件路径

### 2. 后端 API 命令 (`src-tauri/src/commands/logging.rs`)

#### 新增命令

**get_logs**
- 参数: `offset: usize, limit: usize`
- 返回: `LogQueryResponse`
  - `logs`: 日志列表
  - `total`: 总日志数
  - `offset`: 当前偏移量
  - `limit`: 每页数量
  - `has_more`: 是否还有更多日志
- 功能: 分页查询日志

**get_log_file_path**
- 返回: 当前日志文件路径
- 功能: 获取日志文件位置

**get_log_count**
- 返回: 日志总数
- 功能: 快速获取日志数量

### 3. 前端状态管理 (`src/store/logStore.ts`)

#### 新增状态
```typescript
interface LogStore {
  logs: LogMessage[];      // 当前加载的日志
  total: number;           // 总日志数
  hasMore: boolean;        // 是否还有更多
  loading: boolean;        // 加载状态
  // ...
}
```

#### 新增方法
- `loadLogs(offset, limit)`: 加载指定范围的日志
- `loadMoreLogs()`: 加载更多历史日志
- `refreshLogs()`: 刷新当前日志
- `setLogs()`: 设置日志列表
- `appendLogs()`: 追加日志

### 4. 前端 UI 优化 (`src/components/LogView/LogWindow.tsx`)

#### 懒加载实现
- 初始加载 100 条最新日志
- 向上滚动到顶部时自动加载更多历史日志
- 加载时保持滚动位置不变
- 显示加载状态指示器

#### 实时日志
- 继续监听 `log-message` 事件
- 新日志实时追加到列表末尾
- 不影响历史日志的懒加载

#### UI 增强
- 添加刷新按钮（带旋转动画）
- 显示总日志数和"还有更多"提示
- 加载状态指示器（黄色脉动圆点）
- 初始加载时显示加载动画
- 向上滚动时显示"加载中..."提示

## 工作流程

### 日志写入流程
```
1. 应用程序调用 log_exec! 宏
   ↓
2. LogManager.emit_log()
   ↓
3. write_to_file() → 写入文件
   ↓
4. emit("log-message") → 发送到前端
```

### 日志读取流程
```
1. 用户打开日志窗口
   ↓
2. loadLogs(0, 100) → 加载最新 100 条
   ↓
3. 显示日志列表
   ↓
4. 用户向上滚动
   ↓
5. loadMoreLogs() → 加载更多历史日志
   ↓
6. 追加到列表顶部，保持滚动位置
```

### 实时日志流程
```
1. 后端执行图
   ↓
2. 发送 log-message 事件
   ↓
3. 前端监听事件
   ↓
4. addLog() → 追加到列表末尾
   ↓
5. 自动滚动到底部（如果启用）
```

## 性能优化

### 后端优化
- 使用 `BufReader` 高效读取文件
- 分页查询，避免一次性加载所有日志
- 文件立即刷新，确保数据不丢失

### 前端优化
- 懒加载，初始只加载 100 条
- 按需加载更多，减少内存占用
- 虚拟滚动（未来可实现）

## 使用示例

### 打开日志窗口
1. 点击 Window → Logs
2. 自动加载最新 100 条日志
3. 显示总日志数

### 查看历史日志
1. 向上滚动到顶部
2. 自动加载更多历史日志
3. 保持当前滚动位置

### 刷新日志
1. 点击刷新按钮
2. 重新加载当前数量的日志
3. 保持过滤条件

### 实时监控
1. 执行图
2. 新日志自动追加
3. 自动滚动到底部（如果启用）

## 日志文件格式

每行一条 JSON 格式的日志：
```json
{"timestamp":"2026-02-02 12:34:56.789","level":"info","log_type":"execution","message":"执行图开始","source":null}
{"timestamp":"2026-02-02 12:34:56.790","level":"debug","log_type":"execution","message":"执行节点: Print (print)","source":"Print"}
```

## 日志文件位置

- Windows: `logs/app_YYYYMMDD_HHMMSS.log`
- 相对于应用程序根目录
- 每次启动应用创建新文件

## 配置选项

### 前端配置
- 初始加载数量: 100 条
- 每次加载更多: 50 条
- 滚动触发阈值: 距顶部 100px

### 后端配置
- 日志目录: `logs/`
- 文件名格式: `app_{timestamp}.log`
- 编码格式: UTF-8

## 后续优化建议

### 1. 虚拟滚动
- 使用 react-window 或 react-virtualized
- 只渲染可见区域的日志
- 支持数万条日志流畅滚动

### 2. 日志压缩
- 旧日志文件自动压缩
- 减少磁盘占用
- 支持读取压缩文件

### 3. 日志清理
- 自动删除超过 N 天的日志
- 限制日志文件总大小
- 提供手动清理功能

### 4. 日志导出
- 导出为 CSV/JSON 格式
- 支持日期范围筛选
- 支持级别/类型筛选

### 5. 日志搜索
- 全文搜索
- 正则表达式搜索
- 高亮显示匹配项

### 6. 日志分析
- 统计各级别日志数量
- 显示日志趋势图表
- 错误日志聚合

## 相关文件

### 后端
- `src-tauri/src/logging.rs` - 日志管理器（持久化）
- `src-tauri/src/commands/logging.rs` - 日志 API 命令
- `src-tauri/src/commands/mod.rs` - 命令模块注册
- `src-tauri/src/lib.rs` - 命令注册

### 前端
- `src/store/logStore.ts` - 日志状态管理（懒加载）
- `src/components/LogView/LogWindow.tsx` - 日志窗口组件（UI）
- `src/types/logging.ts` - 日志类型定义

## 测试建议

### 功能测试
1. 打开日志窗口，确认加载最新 100 条
2. 向上滚动，确认加载更多历史日志
3. 执行图，确认新日志实时追加
4. 点击刷新，确认重新加载
5. 关闭并重新打开窗口，确认日志持久化

### 性能测试
1. 生成大量日志（1000+ 条）
2. 测试初始加载速度
3. 测试滚动加载速度
4. 测试内存占用
5. 测试文件写入性能

### 边界测试
1. 空日志文件
2. 超大日志文件（10000+ 条）
3. 日志文件损坏
4. 磁盘空间不足
5. 并发写入

## 完成时间
2026-02-02
