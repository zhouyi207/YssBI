---
description: Frontend state persistence rules for localStorage and Zustand
globs: src/**/*.{ts,tsx}
alwaysApply: false
---

# Frontend State Persistence

- 页面缓存中 **需要跨会话保留** 的信息放在 `localStorage`，例如主题、窗口布局、上次打开的页面、折叠状态、用户偏好。
- **临时运行态** 放在 Zustand store 中，例如当前选中的临时 tab、弹层状态、加载中状态、一次性表单草稿、运行中 UI 状态。
- 不要把临时 UI 状态写进 `localStorage`；也不要把需要下次启动恢复的偏好只放在组件 `useState` 里。
- 后端真实状态（设备、模型、管线是否运行等）以后端返回为准，前端 store 只缓存展示态或派生态。

```tsx
// ✅ GOOD: persistent preference
localStorage.setItem("theme", "dark")

// ✅ GOOD: transient UI state
const useUiStore = create((set) => ({
  activePanel: "device",
  setActivePanel: (activePanel) => set({ activePanel }),
}))
```
