---
description: Message notifications must use shadcn toast in the bottom-right corner
alwaysApply: true
---

# Toast Notifications

- 所有普通业务消息提醒、成功提示、失败提示、轻量警告，都使用 shadcn toast 组件实现。
- Toast 位置统一放在 **右下角**，不要在不同页面自行改变位置。
- 不要使用 `window.alert`、`window.confirm`、`window.prompt` 或浏览器原生通知来做业务提示。
- 不要为单个页面手写独立提示浮层；如需扩展样式，应基于项目统一 toast 封装或 shadcn toast 变体。

```tsx
// ✅ GOOD
toast.success("配置已保存")

// ❌ BAD
window.alert("配置已保存")
```
