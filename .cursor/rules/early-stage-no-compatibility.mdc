---
description: 0.x 未发布阶段禁止 legacy/deprecated/兼容层，直接采用当前架构
alwaysApply: true
---

# Early Stage (0.x, Unreleased) — No Compatibility Debt

项目处于 **0.x、从未正式发布** 阶段。架构与存储格式以**当前设计为唯一真源**；不为未发布分支上的旧形态保留兼容路径。

## 禁止

- **兼容层 / 迁移 / fallback reader / shim**：旧 API、旧 storage、旧 command 形状若已废弃，直接删除并改调用方与测试。
- **代码与命名中的 legacy 语义**：避免 `legacy*`、`old*`、`fallback*`（指兼容旧格式）、`compat*`、`migrate*`（仅为过渡）、`deprecated*` 变量/函数/参数名。
- **`@deprecated` / `/** @deprecated */` / Rust `#[deprecated]`**：未发布阶段不要用标注拖延删除；该删就删，该改名就改名。
- **双路径读写**：不要同时维护「新 store + 旧字段回退读」；一次改到目标结构。

## 允许

- **外部真实用户数据**（正式发布后）或用户**明确要求**迁移路径时，再单独设计 migration。
- **领域语义上的 “default / seed / initial”**：描述当前设计的初始值或首次 hydrate，不是「兼容旧版」。
  - ✅ `resolveInitialGraphViewport(..., seedFromFile?)`
  - ❌ `resolveInitialGraphViewport(..., legacyCanvas?)`

## 做法

- 格式/API 错了 → **替换** + 更新测试与 TODO/ADR，而不是叠一层 reader。
- 发现已有 legacy/deprecated 残留 → 顺手清掉，不要在新代码里引用或扩展。

```typescript
// ❌ BAD — 未发布项目仍保留旧图文件 viewport 双写
if (legacyCanvas) applyViewport(legacyCanvas)
persistToGraphFile(viewport)

// ✅ GOOD — 单一 view-state 模型
applyViewport(resolveInitialGraphViewport(graphPath, seedFromFile))
persistGraphViewportMemento(graphPath, viewport)
```

```rust
// ❌ BAD
#[deprecated(note = "use new_api")]
pub fn old_load() { ... }

// ✅ GOOD — 删除 old_load，只保留 new_api
```
