# 日志窗口测试指南

## 修复内容

### 1. 窗口显示问题修复
**问题**: 点击 Logs 菜单项后窗口没有显示

**原因**: LogWindow 组件缺少 `getCurrentWindow().show()` 调用

**修复**:
- 在 `LogWindow` 组件的 `useEffect` 中添加了窗口显示逻辑
- 添加了窗口最大化状态监听
- 添加了自定义标题栏和窗口控制按钮（最小化、最大化、关闭）

### 2. 日志转发功能添加
**问题**: 执行图时没有日志显示在日志窗口中

**原因**: 执行器中没有使用日志宏发送日志到前端

**修复**:
- 在 `src-tauri/src/commands/execution.rs` 中添加了执行开始、收集节点、执行完成的日志
- 在 `src-tauri/src/executor/context.rs` 中添加了执行图开始、节点执行、执行完成的日志

## 测试步骤

### 1. 打开日志窗口
1. 启动应用程序
2. 点击顶部菜单栏的 **Window** 菜单
3. 选择 **Logs** 菜单项
4. 应该会弹出一个独立的日志窗口

### 2. 测试日志显示
1. 在主窗口中创建一个简单的图（例如：Event On Run → Print → Plot）
2. 点击执行按钮
3. 切换到日志窗口
4. 应该能看到以下日志：
   - **执行日志 (EXEC)**: 
     - "开始执行图"
     - "收集了 X 个节点和 Y 个变量"
     - "开始执行图节点"
     - "执行图开始"
     - "从起始节点开始执行: ..."
     - "执行节点: XXX (node_type)"
     - "执行图完成"
     - "图执行完成，生成了 X 条日志"

### 3. 测试日志过滤
1. 点击工具栏的 **过滤器** 按钮
2. 测试以下过滤功能：
   - **搜索框**: 输入关键词搜索日志
   - **级别过滤**: 点击 ERROR、WARN、INFO、DEBUG、TRACE 按钮切换显示
   - **类型过滤**: 点击"应用"、"执行"、"系统"按钮切换显示

### 4. 测试其他功能
- **自动滚动**: 点击向下箭头图标切换自动滚动
- **清空日志**: 点击垃圾桶图标清空所有日志
- **窗口控制**: 测试最小化、最大化、关闭按钮

## 日志类型说明

### 执行日志 (EXEC)
- 颜色: 紫色
- 来源: 图执行过程
- 内容: 节点执行、数据流动、执行状态等

### 应用日志 (APP)
- 颜色: 绿色
- 来源: 应用程序操作
- 内容: 用户操作、UI 事件、应用状态等

### 系统日志 (SYS)
- 颜色: 青色
- 来源: 系统级操作
- 内容: 文件 I/O、网络请求、系统错误等

## 日志级别说明

- **ERROR** (红色): 错误信息
- **WARN** (黄色): 警告信息
- **INFO** (蓝色): 一般信息
- **DEBUG** (灰色): 调试信息
- **TRACE** (深灰色): 跟踪信息

## 已添加的日志点

### 执行命令层 (`commands/execution.rs`)
1. 执行开始: "开始执行图"
2. 收集节点: "收集了 X 个节点和 Y 个变量"
3. 执行完成: "图执行完成，生成了 X 条日志"
4. 执行失败: "图执行失败: {error}"

### 执行上下文层 (`executor/context.rs`)
1. 执行图开始: "执行图开始"
2. 起始节点: "从起始节点开始执行: {node_id}"
3. 节点执行: "执行节点: {node_name} ({node_type})"
4. 执行图完成: "执行图完成"

## 后续优化建议

### 1. 添加更多日志点
- 连接激活时的日志
- 数据传输时的日志
- 错误发生时的详细日志
- 变量读写时的日志

### 2. 日志性能优化
- 限制日志数量（如最多保留 1000 条）
- 添加日志分页功能
- 添加日志导出功能

### 3. 日志增强功能
- 添加日志时间戳筛选
- 添加日志来源筛选（按节点 ID）
- 添加日志高亮显示
- 添加日志详情查看

### 4. 集成到主窗口
- 可以考虑在主窗口底部添加日志面板
- 支持在主窗口和独立窗口之间切换
- 添加快捷键快速打开日志窗口

## 相关文件

### 前端
- `src/components/LogView/LogWindow.tsx` - 日志窗口组件
- `src/store/logStore.ts` - 日志状态管理
- `src/types/logging.ts` - 日志类型定义
- `src/components/Editor/Layout/Menubar.tsx` - 菜单栏（添加了 Logs 菜单项）
- `src/App.tsx` - 应用入口（添加了日志窗口路由）

### 后端
- `src-tauri/src/logging.rs` - 日志管理器和宏定义
- `src-tauri/src/commands/execution.rs` - 执行命令（添加了日志）
- `src-tauri/src/executor/context.rs` - 执行上下文（添加了日志）
- `src-tauri/src/lib.rs` - 应用入口（初始化日志管理器）

## 故障排查

### 问题 1: 日志窗口打开后是黑屏
**解决方案**: 检查浏览器控制台是否有错误，确认 `getCurrentWindow().show()` 被调用

### 问题 2: 没有日志显示
**解决方案**: 
1. 检查后端是否正确初始化了日志管理器
2. 检查执行器中是否使用了 `log_exec!` 宏
3. 检查前端是否正确监听了 `log-message` 事件

### 问题 3: 日志显示不完整
**解决方案**: 检查日志过滤器设置，确保所有级别和类型都被启用

### 问题 4: 日志窗口无法关闭
**解决方案**: 检查窗口控制按钮的事件处理函数是否正确

## 完成时间
2026-02-02
