# 严格类型系统修复完成

## 修复原则
使用**严格的类型系统**，不自动转换类型。如果类型不匹配，用户需要显式使用类型转换节点。

## 修复的节点

### 1. Print 节点
**文件**: `src-tauri/src/executor/node/catalog/debug.rs`

**修改前**:
```rust
print_node.add_in_data_pin(GenericInDataPin::new(
    uuid::Uuid::nil(),
    "Value",
    PinTypeDesc::unknown()  // ❌ 接受任意类型
));
```

**修改后**:
```rust
print_node.add_in_data_pin(GenericInDataPin::new(
    uuid::Uuid::nil(),
    "Value",
    PinTypeDesc::concrete(ValueType::String)  // ✅ 只接受 String
));
```

**影响**:
- 之前：可以直接连接任意类型到 Print
- 现在：只能连接 String 类型
- 解决：用户需要先使用 ToString 转换节点

**示例**:
```
错误: Int → Print ❌
正确: Int → ToString → Print ✅
```

---

### 2. Branch 节点（已经正确）
**文件**: `src-tauri/src/executor/node/catalog/control.rs`

**当前定义**:
```rust
branch.add_in_data_pin(GenericInDataPin::new(
    uuid::Uuid::nil(), 
    "Condition", 
    PinTypeDesc::concrete(ValueType::Boolean)  // ✅ 只接受 Boolean
));
branch.add_in_data_pin(GenericInDataPin::new(
    uuid::Uuid::nil(), 
    "TrueValue", 
    PinTypeDesc::any()  // ✅ 可以是任意类型
));
branch.add_in_data_pin(GenericInDataPin::new(
    uuid::Uuid::nil(), 
    "FalseValue", 
    PinTypeDesc::any()  // ✅ 可以是任意类型
));
```

**说明**:
- `Condition`: 只接受 Boolean 类型 ✅
- `TrueValue/FalseValue`: 可以是任意类型 ✅（因为是传递值，不是判断条件）
- 这个设计是正确的，无需修改

**示例**:
```
错误: Int → Branch.Condition ❌
正确: Int → ToBool → Branch.Condition ✅
```

---

### 3. Min Max Average 节点
**文件**: `src-tauri/src/executor/node/catalog/math/multi_output.rs`

**修改**:
```rust
node.add_in_data_pin(GenericInDataPin::new(
    uuid::Uuid::nil(), 
    "Array", 
    PinTypeDesc::concrete(ValueType::List(Box::new(ValueType::Float64)))  // ✅ 只接受数字数组
));
```

**影响**:
- 只能连接 `Float64` 数组
- 其他类型需要先转换

---

### 4. Statistics 节点
**文件**: `src-tauri/src/executor/node/catalog/math/multi_output.rs`

**修改**:
```rust
node.add_in_data_pin(GenericInDataPin::new(
    uuid::Uuid::nil(), 
    "Array", 
    PinTypeDesc::concrete(ValueType::List(Box::new(ValueType::Float64)))  // ✅ 只接受数字数组
));
```

---

### 5. Split String 节点
**文件**: `src-tauri/src/executor/node/catalog/string_multi_output.rs`

**修改**:
```rust
node.add_out_data_pin(GenericOutDataPin::new(
    uuid::Uuid::nil(), 
    "Array", 
    PinTypeDesc::concrete(ValueType::List(Box::new(ValueType::String)))  // ✅ 返回字符串数组
));
```

---

## 类型系统总结

### 确定类型节点（严格类型检查）

#### Debug 节点
- **Print**: `String` - 只接受字符串

#### Math 节点
- **Min Max Average**: `List(Float64)` - 只接受数字数组
- **Statistics**: `List(Float64)` - 只接受数字数组
- **Add, Sub, Mul, Div**: 使用类型推断，但只支持数字类型

#### String 节点
- **Split String**: 输出 `List(String)` - 返回字符串数组
- **Concat**: `String` - 只接受字符串

#### Control 节点
- **Branch.Condition**: `Boolean` - 只接受布尔值
- **IfElse.Condition**: `Boolean` - 只接受布尔值
- **WhileLoop.Condition**: `Boolean` - 只接受布尔值

### 泛型类型节点（保持灵活）

#### 容器操作
- **Array Info**: `List(Any)` - 可以处理任意类型数组
- **Partition Array**: `List(Any)` - 可以分割任意类型数组
- **Filter Array**: `List(Any)` - 可以过滤任意类型数组

#### 控制流
- **Branch.TrueValue/FalseValue**: `Any` - 可以传递任意类型
- **For Each.Item**: `Any` - 可以遍历任意类型

---

## 需要的类型转换节点

为了支持严格类型系统，需要添加以下转换节点：

### 1. ToString 节点
```rust
// 输入: Any
// 输出: String
// 功能: 将任意类型转换为字符串
```

**用途**:
- Int → ToString → Print
- Float → ToString → Print
- Bool → ToString → Print

### 2. ToBool 节点
```rust
// 输入: Any
// 输出: Boolean
// 功能: 将任意类型转换为布尔值
```

**用途**:
- Int → ToBool → Branch.Condition
- String → ToBool → IfElse.Condition

**转换规则**:
- 数字: 0 = false, 非0 = true
- 字符串: "" = false, 非空 = true
- 数组: [] = false, 非空 = true

### 3. ToInt 节点
```rust
// 输入: String | Float | Boolean
// 输出: Int
// 功能: 转换为整数
```

**用途**:
- String "123" → ToInt → 123
- Float 3.14 → ToInt → 3
- Bool true → ToInt → 1

### 4. ToFloat 节点
```rust
// 输入: String | Int | Boolean
// 输出: Float64
// 功能: 转换为浮点数
```

**用途**:
- String "3.14" → ToFloat → 3.14
- Int 3 → ToFloat → 3.0
- Bool true → ToFloat → 1.0

### 5. ToArray 节点
```rust
// 输入: Any
// 输出: List(Any)
// 功能: 将单个值包装为数组
```

**用途**:
- Int 5 → ToArray → [5]
- String "hello" → ToArray → ["hello"]

---

## 使用示例

### 示例 1: 打印数字
**错误方式**:
```
Constant(42) → Print ❌ (类型不匹配: Int vs String)
```

**正确方式**:
```
Constant(42) → ToString → Print ✅
```

### 示例 2: 条件判断
**错误方式**:
```
Constant(1) → Branch.Condition ❌ (类型不匹配: Int vs Boolean)
```

**正确方式**:
```
Constant(1) → ToBool → Branch.Condition ✅
```

### 示例 3: 数学运算
**错误方式**:
```
String["1", "2", "3"] → Min Max Average ❌ (类型不匹配: List(String) vs List(Float64))
```

**正确方式**:
```
需要先将字符串数组转换为数字数组（需要 MapArray + ToFloat 节点）
```

---

## 优势

### 1. 类型安全
- 编译时发现类型错误
- 不会出现运行时类型转换失败
- 更可靠的执行

### 2. 明确的意图
- 用户明确知道需要什么类型
- 显式的类型转换表达清晰的意图
- 减少隐式行为带来的困惑

### 3. 更好的错误提示
- 连接时就能发现类型不匹配
- 提示用户需要什么类型转换
- 减少调试时间

### 4. 更容易维护
- 类型系统清晰明确
- 不需要处理各种隐式转换
- 代码更容易理解

---

## 破坏性变更

### 影响的用户场景

#### 1. Print 节点
**之前**: 可以直接打印任意类型
```
Constant(42) → Print  // 自动转换为 "42"
```

**现在**: 必须先转换为字符串
```
Constant(42) → ToString → Print
```

#### 2. Branch 节点
**之前**: 可以直接使用数字作为条件（如果有自动转换）
```
Constant(1) → Branch.Condition  // 自动转换为 true
```

**现在**: 必须先转换为布尔值
```
Constant(1) → ToBool → Branch.Condition
```

#### 3. Math 节点
**之前**: 可能接受混合类型数组（如果有自动转换）
```
["1", "2", "3"] → Min Max Average  // 自动转换为数字
```

**现在**: 必须是数字数组
```
需要先转换为数字数组
```

---

## 迁移指南

### 步骤 1: 识别类型不匹配
运行现有的图，查看哪些连接出现类型错误。

### 步骤 2: 添加类型转换节点
在类型不匹配的地方插入适当的转换节点：
- 需要字符串 → 添加 ToString
- 需要布尔值 → 添加 ToBool
- 需要数字 → 添加 ToInt 或 ToFloat

### 步骤 3: 测试验证
确保转换后的图能正常工作。

---

## 下一步工作

### 1. 实现类型转换节点 ⚠️ 紧急
需要立即实现以下节点：
- ToString
- ToBool
- ToInt
- ToFloat
- ToArray

### 2. 前端类型检查 ⚠️ 重要
在前端添加连接时的类型检查：
- 检查源和目标 pin 的类型兼容性
- 显示友好的错误提示
- 建议可能的类型转换节点

### 3. 自动插入转换节点 💡 可选
智能地自动插入类型转换节点：
- 用户连接不兼容类型时
- 自动提示或插入转换节点
- 提高用户体验

### 4. 文档更新 📝 必要
更新所有节点的文档：
- 说明每个 pin 的确切类型
- 提供类型转换示例
- 解释类型系统的工作原理

---

## 编译状态
✅ **编译成功**
- 只有 8 个警告（与此修复无关）
- 无编译错误

## 文件修改
1. `src-tauri/src/executor/node/catalog/debug.rs` - Print 节点改为 String
2. `src-tauri/src/executor/node/catalog/math/multi_output.rs` - Math 节点改为 Float64
3. `src-tauri/src/executor/node/catalog/string_multi_output.rs` - Split String 改为 String 数组

## 状态
✅ **修复完成**
⚠️ **需要添加类型转换节点才能完整使用**

现在系统使用严格的类型检查，Print 只接受 String，Branch.Condition 只接受 Boolean，Math 节点只接受数字。用户需要显式使用类型转换节点来处理类型不匹配。
