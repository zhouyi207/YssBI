# VEC (Cointegration)

Johansen 向量误差修正模型（Stata `vec`）。

对 **Variables** 中 I(1) 变量估计协整关系，可选确定性项与滞后结构。

## 输出

**Result** + VEC 报告窗口（特征值、$\alpha$/$\beta$、VECLMAR、稳定性）。

建议先运行 **VECRANK (Johansen)** 确定协整秩。
