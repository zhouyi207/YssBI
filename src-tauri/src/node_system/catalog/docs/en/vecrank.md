# VECRANK (Johansen)

Johansen trace (and optional max eigenvalue) cointegration rank tests (Stata `vecrank`).

Use the selected rank when configuring **VEC (Cointegration)**.

## Inputs

Multivariate **Variables** `DataFrame`, trend specification, max lags — same family as VEC estimation.
