# Boolean Constant

Emits a fixed `Boolean` value configured on the node (no input pins).

## Usage

Supply flags to **Branch**, **And**/**Or**, or **Set Variable**. Change the literal in the node UI without rewiring.
