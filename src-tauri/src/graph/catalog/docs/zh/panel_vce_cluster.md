# VCE: Cluster (by Entity)

面板默认 VCE：使用面板节点上的 **Entity ID** 做聚类稳健标准误（无需单独连接序列 pin）。

## 用法

**VCE** → **Panel Configure** → **Config** → **Panel Summary** / **Panel DID**。

等价于对面板个体的一维聚类；除非改用非 cluster 的 VCE，否则不对时间维单独聚类。
