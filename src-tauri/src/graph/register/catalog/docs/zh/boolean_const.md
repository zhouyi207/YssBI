# Boolean Constant（布尔常量）

输出节点上配置的固定 `Boolean` 值（无输入引脚）。

## 输入

| Pin | 说明 |
|-----|------|
| *（编辑器）* **Value** | 节点本体上可编辑的 `true` / `false` |

## 输出

| Pin | 说明 |
|-----|------|
| **Value** | 常量 `Boolean` |

## 用法

为 **Branch**、**And**/**Or** 或 **Set Variable** 提供标志位。在节点 UI 中改字面值即可，无需改连线。
