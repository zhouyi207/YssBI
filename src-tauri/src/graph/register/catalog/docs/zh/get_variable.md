# Get Variable（读取变量）

读取在节点检查器中选定的项目**变量**的当前值。

## 输入

| Pin | 说明 |
|-----|------|
| *（检查器）* **Variable** | 要读取的变量（非图引脚） |

## 输出

| Pin | 说明 |
|-----|------|
| **Value** | 变量当前内容（`Any` 类型；schema 随变量解析） |

## 用法

在节点属性中选择变量，将 **Value** 连到下游（**Convert**、回归 **Y**/**X** 等）。每次图运行都会读取最新存储值。
