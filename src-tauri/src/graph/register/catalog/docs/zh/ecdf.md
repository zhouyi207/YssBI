# ECDF

由数值 **Values** 绘制经验累积分布函数 $\hat F(x)$：排序后第 $i$ 个观测对应 $y = i/n$。

## Pin

| Pin | 方向 | 说明 |
|-----|------|------|
| **In** | 执行输入 | 控制流入口 |
| **Values** | 输入 | `Float64` 或 `Int64` `DataSeries` |
| **Out** | 执行输出 | 控制流出口 |

## 用法

执行后打开 **Plot** 窗口显示 ECDF 阶梯曲线。null 与非有限值剔除；须至少 1 个有效观测。
