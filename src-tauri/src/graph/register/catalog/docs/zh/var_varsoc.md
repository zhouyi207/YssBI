# VAR Lag Order (varsoc)

VAR 滞后阶选择（Stata `varsoc varlist, maxlag(#)`）。

对 $0,\ldots,p_{\max}$ 报告 LL、LR、FPE、AIC、HQIC、SBIC，供 **VAR Summary** 前选定 $p$。

## 输入

与 VAR Summary 相同的内生 **Variables** 布局，另需 **Max Lag**。
