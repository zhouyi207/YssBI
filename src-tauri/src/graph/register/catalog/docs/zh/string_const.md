# String Constant（字符串常量）

输出节点上配置的固定文本（无输入引脚）。

## 输入

| Pin | 说明 |
|-----|------|
| *（编辑器）* **Value** | 节点本体上可编辑的字符串字面量 |

## 输出

| Pin | 说明 |
|-----|------|
| **Value** | 常量 `String` |

## 用法

提供标签、列名或可解析字面量。下游需要数值时可配合 **Convert** 或 **String to Float64**。
