# 获取常量

输出当前事件或函数中定义的常量。在图的详情面板中添加、编辑常量，然后在节点参数中选择常量，也可以直接从详情面板插入引用节点。

输出类型跟随所选常量。支持标量、数组、对象、DataFrame 和 DataSeries；表格数据随图文档一起保存。

常量在一次运行中保持不变。编辑常量会修改图草稿，重新编译后使用新值，保存后写入文件。重命名不会影响引用。删除已被引用的常量会产生图问题，需要重新选择常量或移除对应节点。
