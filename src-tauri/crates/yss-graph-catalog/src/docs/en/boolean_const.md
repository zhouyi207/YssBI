# Boolean Constant

Emits a fixed `Boolean` value configured on the node (no input pins).

## Usage

Supply flags to **And**/**Or**, filters, or other data transformations. Change the literal in the node UI without rewiring.
