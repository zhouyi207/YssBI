# Panel Fit

Connect `response, predictors, entity, time` and set estimation parameters in Detail → Parameters. Outputs the fitted `model`, `fitted` values and `residuals`. Connect `model` to the corresponding Summary.

This node has a catalog contract; its execution kernel is not yet registered.
