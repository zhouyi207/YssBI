# IV LIML Fit

Connect `response, predictors, endogenous, instruments` and set estimation parameters in Detail → Parameters. Outputs the fitted `model`, `fitted` values and `residuals`. Connect `model` to the corresponding Summary.

This node has a catalog contract; its execution kernel is not yet registered.
