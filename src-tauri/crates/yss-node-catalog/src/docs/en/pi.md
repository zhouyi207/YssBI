# Pi (π)

Requires no inputs or parameters. Produces a Numeric scalar for pi, approximately `3.141592653589793`.

This fixed mathematical constant does not require a custom graph constant. Connect it to arithmetic nodes; series operations broadcast it according to the receiving node's rules. The value is a Float64 approximation, not an arbitrary-precision symbolic value.
