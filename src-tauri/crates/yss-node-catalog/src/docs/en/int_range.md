# Int Range

Builds an **Int64** `DataSeries` from **Start** to **End** (exclusive), advancing by **Step**.

## Usage

Set **Start**, **End**, and **Step** in **Detail → Parameters**. Defaults are 0, 10, and 1. The canvas exposes the **DataSeries** output for row indices, counters, or panel IDs.
