# Euler's Number (e)

Requires no inputs or parameters. Produces a Numeric scalar for Euler's number, approximately `2.718281828459045`.

Connect it to Power's base input to calculate `e ^ x`, or to Logarithm's base input for natural logarithms. No custom graph constant is required. The output is a Float64 approximation; downstream nodes handle scalar broadcasting.
