# Standardize DataSeries

按 z = (x − 均值) / 标准差计算，输出标准化序列、均值和样本标准差（ddof = 1）。Null 不参与统计，输出中保留原位置。非空样本不足两个、方差为零或统计量非有限时报错。还原时将两个标量输出连接到逆标准化节点。

关系数列先扫描统计量，再以保留原行域的表达式输出，能够与原数列比较。内存数列仍返回内存数列。浮点标准化及还原可能有舍入误差，请用“等于”的容差模式校验。
