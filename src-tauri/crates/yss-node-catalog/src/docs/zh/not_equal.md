# Not Equal（!=）

判断两值是否不等：

$$
\text{Result} = (A \neq B)
$$

使用与“等于”相同的逐元素比较和广播规则。两个标量返回二元标量；任一输入为序列时返回二元序列。对非空比较结果取反，空值仍为空值。数值表示按精确数值比较，文本不隐式转为数字。内存序列必须等长，惰性序列必须属于同一关系行域。

## 用法

在值不同时生成布尔掩码，供过滤或下游数据转换使用。
