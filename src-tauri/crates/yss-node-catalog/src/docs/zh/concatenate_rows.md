# 按行拼接

按输入端口顺序纵向追加两个或更多惰性数据帧，保留重复行及各输入表内部的行顺序。可增加数据帧输入端口。

- **按列名对齐**（默认）：同名列对应，缺失列补空值；新列按首次出现的顺序追加。
- **按列位置对齐**：输入列数必须相同，输出沿用第一个表的列名及顺序。

对应列须具有兼容的语义定义和相同物理类型，不隐式进行有损转换。输入必须来自同一项目会话，不能混用同一数据集的不同快照。计算在读取结果时执行，不修改源数据集。
