# Add (+)

逐元素加法，标量可广播：

$$
\text{Result} = a + b + \cdots
$$

任一操作数为 `DataSeries` 时，标量会广播到序列长度，结果为 `DataSeries`。仅在至少一个操作数为 `Float64` 时才把 `Int64` 提升为 `Float64`；纯标量输入按相同规则输出标量。

## 用法

将两个及以上标量或序列接到操作数引脚。标量与序列混用可对整列加偏移。形状与元素类型独立求解：序列宽于标量，`Float64` 宽于 `Int64`。
