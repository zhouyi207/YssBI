# Erlang

Erlang 分布为 **Gamma** 的特例：形状参数 **K** 为正整数，表示 **K** 个独立同分布指数等待时间之和：

$$
X=\sum_{i=1}^{K} \mathrm{Exp}(\lambda)
$$

## 用法

在 **Detail → 配置** 中设置分布参数和样本数，画布上保留 **Samples** 数据输出。

整数形状范围为 [1, 2^53]，率参数必须有限且为正。采用 Gamma 的形状/率参数化，输出正 Float64 样本。
