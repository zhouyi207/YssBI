# 筛选列

从输入数据帧中保留指定的列，输出列按“列”参数中的顺序排列。例如输入包含 `a、b、c`，选择 `["c", "a"]` 后，输出依次为 `c、a` 两列。

通过 Source 输入数据帧，在 Columns（列）参数中指定至少一个现有列名；列名不能重复。Result 输出筛选后的数据帧，保留原有行及行顺序。

此节点用于选择和重排列；筛选行请使用“筛选行”，修改列名请使用“重命名数据框”。操作生成新的计算结果，不修改源数据集。
