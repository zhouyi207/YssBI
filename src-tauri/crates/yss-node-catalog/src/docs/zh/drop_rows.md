# 移除行

移除满足指定条件的行。例如条件为 `金额 < 0` 时，删除金额为负数的记录，保留其余记录和全部列。

通过 Source 输入数据帧，在 Predicate（条件）中选择列、运算符及比较值。仅移除条件结果为真的行；条件为假或 NULL 的行保留。若需要移除缺失值，使用“为空”条件；空值检查无需比较值。

Result 输出新的数据帧，不修改源数据集。此节点按条件移除行，不按行号删除；需要保留匹配行时，请使用“筛选行”。
