# Equal（==）

判断两值是否相等：

$$
\text{Result} = (A = B)
$$

比较两个共享输入类型的值。Numeric 按精确数值比较，整数、无符号整数与浮点表示可相等，不使用 epsilon，也不通过有损浮点转换比较大整数。列表和记录逐项使用相同规则；其他值按值或资源身份比较。输出为单个 `Boolean`（非 `DataSeries`）。

## 用法

为筛选生成布尔条件，或与 **And** / **Or** 组合。序列逐元素比较请用 **DataSeries** 专用比较节点。
