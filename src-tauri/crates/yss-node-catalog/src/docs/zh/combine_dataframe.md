# 组装数据帧

将一个或多个数据序列作为列组装成数据帧。输入可增加，列顺序按输入端口顺序排列；允许不同列具有不同语义类型。

## 用法

来自同一行域的惰性数列会形成惰性列投影，在消费结果时执行。筛选列、重命名后的同一来源数列也可组合；筛选行或截取行后的数列不能与原行域按长度猜测对齐。

内存数列必须全部等长，保持已物化形式；不以空值补齐短列，也不混用内存数列和惰性数列。列名从来源列或节点输出推导，重名自动加后缀，之后可用重命名节点调整。
