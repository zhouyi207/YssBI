# Uniform

连续均匀分布 $\mathrm{Uniform}(a,b)$，在区间 $[a,b)$ 上密度恒定：

$$
f(x)=\frac{1}{b-a},\quad a \le x < b
$$

## 用法

在 Detail 的**分布参数**和**采样设置**两组中设置分布参数和样本数，画布上保留 **Samples** 数据输出。

上下界必须有限，且下界小于上界、区间宽度有限。采样区间为 [下界, 上界)，不包含上界。
