# 连接

根据指定键匹配左右两个惰性数据帧。分别选择左表和右表的连接键，支持多列键，两侧列数与顺序必须一一对应。

- **内连接**：只保留匹配组合。
- **左连接 / 右连接**：保留指定一侧全部行，未匹配侧补空值。
- **全外连接**：保留双方全部行，未匹配侧补空值。

空键不匹配；重复键会产生所有匹配组合，不自动去重。两侧连接键列都保留，右侧重名列默认添加 `_right`，可以修改该后缀。

连接键需具有兼容语义及相同物理类型。输出按左侧行位置、右侧行位置稳定排列，右侧独有行排在末尾。输入来自同一项目会话，计算和来源租约保留到结果消费结束，不修改源数据集。
