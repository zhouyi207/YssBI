# Ln（自然对数）

逐元素自然对数：

$$
\text{Result} = \ln x
$$

**定义域：** $x > 0$。标量和数列中的非正输入均报错。接受数值标量或数值数据序列；输出使用 Float64 实数表示，形状与输入一致。

标量直接计算，内存数列逐元素计算，惰性数列在消费结果时按批执行。空值、非法定义域、非有限输入/结果以及无法精确提升为 Float64 的整数均报错，不自动生成 null；不会修改源数据集。

## 用法

对严格为正的序列做对数变换（如收入、价格）。若存在非正值，请在上游过滤或截断。
