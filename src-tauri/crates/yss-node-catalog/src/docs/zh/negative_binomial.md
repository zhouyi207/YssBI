# Negative Binomial

负二项分布 $\mathrm{NB}(r,p)$ 表示达到第 $r$ 次成功之前所需的失败次数：

$$
P(X=k)=\binom{k+r-1}{k}(1-p)^k p^r,\quad k=0,1,2,\ldots
$$

## 用法

在 **Detail → 配置** 中设置分布参数和样本数，画布上保留 **Samples** 数据输出。

成功次数为正实数形状 r，成功概率要求 0 < p ≤ 1。输出 Int64 失败次数，不计成功次数；p = 1 时返回零。样本超过 2^53 的精确计数范围时报错。
