# Sqrt（平方根）

逐元素平方根：

$$
\text{Result} = \sqrt{x}
$$

**定义域：** $x \geq 0$，零的平方根为零。标量和数列中的负值均报错。接受数值标量或数值数据序列；输出使用 Float64 实数表示，形状与输入一致。

标量直接计算，内存数列逐元素计算，惰性数列在消费结果时按批执行。空值、非法定义域、非有限输入/结果以及无法精确提升为 Float64 的整数均报错，不自动生成 null；不会修改源数据集。

## 用法

将方差类或平方量还原到原单位。必要时在上游截断负值。
