# 对数

计算指定底数的对数：`结果 = log_底数(真数)`。左侧 Value 输入真数，右侧 Base 输入底数，均可填写数值或连线。结果使用 Float64 实数表示，例如 `log_2(8) = 3`、`log_10(100) = 2`。

真数必须大于零，底数必须大于零且不等于 1；底数也可以在 0 与 1 之间。两个标量返回标量，任一侧数列与另一侧标量可广播，两侧数列逐元素运算。

内存数列必须等长，惰性数列必须属于同一关系行域。空值、非法定义域、非有限输入、无法精确提升为 Float64 的整数及非有限结果会报错。惰性运算在结果消费时执行，不修改源数据集。
