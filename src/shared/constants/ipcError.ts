/** Stable cross-layer identity for errors normalized by the IPC service adapter. */
export const IPC_ERROR_BRAND = Symbol.for("yssbi.ipc-error");

export const IPC_TRANSPORT_FAILURE_CODE = "ipc_transport_failure";
export const IPC_MALFORMED_ERROR_CODE = "ipc_malformed_error";
