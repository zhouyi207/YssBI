/** Shared header strip for Sidebar / TabBar / Detail — one row, one height. */
export const workbenchPanelHeaderClass =
  "box-border flex h-[var(--titlebar-height)] min-h-[var(--titlebar-height)] max-h-[var(--titlebar-height)] shrink-0 items-center border-b border-[var(--strong-border)] bg-[var(--panel-header-bg)] px-3";

/** Shared title typography for Sidebar / Detail panel headers. */
export const workbenchPanelHeaderTitleClass =
  "font-heading text-[10px] font-semibold uppercase tracking-[0.14em] text-muted-foreground";
