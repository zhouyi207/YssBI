import { SidebarDragOverlay } from "../ui/dnd/SidebarDragOverlay";

/** Canvas/sidebar resource drag overlay; Dockview renders editor panel drag ghosts. */
export function RootDockviewDragOverlay() {
  return <SidebarDragOverlay />;
}
