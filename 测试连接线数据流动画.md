# 测试连接线数据流动画

## 功能说明
在执行图时，活跃的连接线上会显示发光的粒子动画，模拟数据从源节点流向目标节点的过程。

## 快速测试步骤

### 测试 1: 基础功能
1. 创建一个简单的图：`Constant` → `Print`
2. 点击执行按钮
3. **预期效果**：
   - 连接线变为黄色
   - 黄色发光粒子从 Constant 流向 Print
   - 粒子平滑移动，到达终点后消失
   - 执行完成后粒子停止

### 测试 2: 多个连接
1. 创建包含多个节点和连接的图
2. 执行图
3. **预期效果**：
   - 所有活跃连接同时显示粒子流动
   - 每条连接的粒子独立移动
   - 不同连接的粒子不会互相干扰

### 测试 3: 缩放测试
1. 创建一个图并执行
2. 在执行过程中放大/缩小画布
3. **预期效果**：
   - 粒子大小随缩放比例调整
   - 在所有缩放级别下都清晰可见
   - 动画保持流畅

### 测试 4: 性能测试
1. 创建一个大型图（50+ 节点）
2. 执行图
3. **预期效果**：
   - 动画流畅，无卡顿
   - 帧率稳定
   - 无性能问题

## 视觉效果说明

### 粒子外观
- **颜色**：黄色 (#facc15)
- **大小**：3-5 像素（随机变化）
- **发光效果**：带有阴影模糊的发光效果
- **速度**：约 1-2 秒穿过整条连接线

### 连接线外观（活跃时）
- **颜色**：黄色（替代原有的类型颜色）
- **粗细**：3 像素（比非活跃状态粗 1 像素）
- **虚线动画**：虚线向后流动（在 Edge.tsx 中实现）
- **外发光**：模糊的外发光效果

### 粒子行为
- **密度**：每条连接最多 3 个粒子
- **生成**：在连接起点随机生成
- **移动**：沿贝塞尔曲线平滑移动
- **消失**：到达终点后自动移除

## 技术细节

### 实现位置
- 文件：`src/components/Editor/Canvas/EdgesLayer.tsx`
- 使用 Canvas 2D API 绘制粒子
- 使用 requestAnimationFrame 实现动画循环
- 与执行状态系统集成

### 性能优化
- 每条连接限制最多 3 个粒子
- 粒子生成概率控制（10% 每帧）
- 使用 delta time 确保帧率无关的平滑动画
- GPU 加速的 Canvas 渲染

## 已知限制

1. **粒子密度**：固定为每条连接最多 3 个
2. **生成速率**：固定为每帧 10% 概率
3. **颜色**：固定为黄色（未来可以根据数据类型变化）
4. **开关控制**：目前没有 UI 开关（始终启用）

## 未来改进方向

1. **类型相关颜色**：不同数据类型使用不同颜色的粒子
2. **用户设置**：添加开关控制、速度调整、密度调整
3. **粒子形状**：不同数据类型使用不同形状
4. **拖尾效果**：粒子后面添加渐隐的拖尾
5. **数据量可视化**：根据数据大小调整粒子大小或数量

## 状态
✅ **实现完成，可以测试**

所有代码已实现并通过 TypeScript 检查，无编译错误。
