# TODOLIST

## 📌 优先级：高 (紧急/核心功能)
- [ ] **撤销/重做系统**：基于已实现的序列化逻辑，添加撤销/重做 (Undo/Redo) 功能。
- [ ] **快捷键支持**：实现 `Ctrl + S` (保存), `Ctrl + O` (导入), `Ctrl + Z/Y` (撤销重做) 等全局快捷键。
- [ ] **连线交互增强**：
    - [ ] 支持点击连线并按 `Delete` 删除。
    - [ ] 连线自动路径优化 (避免穿过节点)。
- [ ] **属性编辑器**：点击节点时，在侧边栏显示其详细属性并允许编辑（如变量名、常量值）。

## 🛠️ 已完成 (Done)
- [x] **节点删除**：选中节点后按键盘 `Delete` 键可删除节点及其所有关联连线。
- [x] **文件导入/导出 (Tauri 原生)**：
    - [x] 接入 Tauri Dialog 和 FS 插件。
    - [x] 支持自定义导出路径和文件名。
    - [x] 规范化 `v1.0.0` 数据协议 (`io.ts`)。
- [x] **状态管理重构**：将节点状态提升至 `CanvasProvider`，修复了跨组件更新产生的 React 渲染警告。
- [x] **专业菜单栏**：新增顶部 `Menubar` 组件，集成导入导出图标和交互反馈。

## 🎨 前端 (视觉与 UX)
- [ ] **视觉修复**：解决毛玻璃的残影无法解决的问题 - 2026.01.17。
- [ ] **节点搜索**：在节点调色板 (Palette) 中添加搜索过滤功能。
- [ ] **自动吸附**：节点拖拽时支持网格对齐。

## ⚙️ 后端 (Rust/Tauri)
- [ ] **本地设置存储**：使用插件存储用户偏好（如画布缩放比例、上次打开的文件路径）。
- [ ] **Rust 逻辑计算层**：考虑将复杂的图计算或验证逻辑移至 Rust 端以提升性能。
