# 日志系统实现完成

## 功能概述
实现了一个完整的日志系统，将后端日志（应用程序日志、执行日志、系统日志）通过事件转发到前端，并在前端显示一个功能丰富的日志窗口。

## 后端实现

### 1. 日志管理器 (`src-tauri/src/logging.rs`)

#### 日志级别
```rust
pub enum LogLevel {
    Trace,   // 跟踪
    Debug,   // 调试
    Info,    // 信息
    Warn,    // 警告
    Error,   // 错误
}
```

#### 日志类型
```rust
pub enum LogType {
    Application,  // 应用程序日志
    Execution,    // 执行日志
    System,       // 系统日志
}
```

#### 日志消息结构
```rust
pub struct LogMessage {
    pub timestamp: String,      // 时间戳
    pub level: LogLevel,        // 日志级别
    pub log_type: LogType,      // 日志类型
    pub message: String,        // 日志消息
    pub source: Option<String>, // 日志来源（节点ID、模块名等）
}
```

#### 使用方法

**方式 1: 使用宏（推荐）**
```rust
use crate::logging::LogLevel;

// 应用程序日志
log_app!(LogLevel::Info, "应用启动");
log_app!(LogLevel::Error, "加载失败", "ProjectLoader");

// 执行日志
log_exec!(LogLevel::Info, "开始执行图");
log_exec!(LogLevel::Debug, "节点执行完成", "node-123");

// 系统日志
log_sys!(LogLevel::Warn, "内存使用率高");
```

**方式 2: 直接调用**
```rust
if let Some(manager) = get_log_manager() {
    manager.log_app(
        LogLevel::Info,
        "应用启动".to_string(),
        None
    );
}
```

### 2. 初始化 (`src-tauri/src/lib.rs`)

在应用启动时初始化日志管理器：
```rust
.setup(|app| {
    // 初始化日志管理器
    logging::init_log_manager(app.handle().clone());
    Ok(())
})
```

### 3. 事件发送

日志通过 Tauri 事件系统发送到前端：
```rust
// 事件名称: "log-message"
// 事件数据: LogMessage
```

---

## 前端实现

### 1. 类型定义 (`src/types/logging.ts`)

```typescript
export type LogLevel = 'trace' | 'debug' | 'info' | 'warn' | 'error';
export type LogType = 'application' | 'execution' | 'system';

export interface LogMessage {
  timestamp: string;
  level: LogLevel;
  log_type: LogType;
  message: string;
  source?: string;
}
```

### 2. 日志存储 (`src/store/logStore.ts`)

使用 Zustand 管理日志状态：
```typescript
const useLogStore = create<LogStore>((set, get) => ({
  logs: [],                    // 所有日志
  filter: initialFilter,       // 过滤器
  maxLogs: 1000,              // 最多保留 1000 条
  
  addLog: (log) => { ... },   // 添加日志
  clearLogs: () => { ... },   // 清空日志
  toggleLevel: (level) => { ... },  // 切换级别过滤
  toggleType: (type) => { ... },    // 切换类型过滤
  setSearchText: (text) => { ... }, // 设置搜索文本
  getFilteredLogs: () => { ... },   // 获取过滤后的日志
}));
```

### 3. 日志窗口组件 (`src/components/LogView/LogWindow.tsx`)

#### 功能特性

1. **实时日志显示**
   - 监听后端 `log-message` 事件
   - 自动添加到日志列表
   - 自动滚动到最新日志

2. **日志过滤**
   - 按级别过滤（Trace, Debug, Info, Warn, Error）
   - 按类型过滤（Application, Execution, System）
   - 文本搜索（搜索消息和来源）

3. **日志管理**
   - 清空所有日志
   - 自动滚动开关
   - 显示日志数量统计

4. **视觉设计**
   - 不同级别使用不同颜色
   - 不同类型使用不同标识
   - 时间戳、级别、类型、来源、消息分列显示
   - 悬停高亮

#### 颜色方案

**日志级别颜色**:
- Error: 红色 (`text-red-400`)
- Warn: 黄色 (`text-yellow-400`)
- Info: 蓝色 (`text-blue-400`)
- Debug: 灰色 (`text-gray-400`)
- Trace: 深灰 (`text-gray-500`)

**日志类型颜色**:
- Application: 绿色 (`text-green-400`)
- Execution: 紫色 (`text-purple-400`)
- System: 青色 (`text-cyan-400`)

---

## 使用示例

### 后端发送日志

#### 示例 1: 应用程序日志
```rust
use crate::logging::LogLevel;

// 应用启动
log_app!(LogLevel::Info, "YssBI 应用已启动");

// 项目加载
log_app!(LogLevel::Info, "正在加载项目", "ProjectLoader");

// 加载失败
log_app!(LogLevel::Error, "项目加载失败: 文件不存在", "ProjectLoader");
```

#### 示例 2: 执行日志
```rust
// 开始执行
log_exec!(LogLevel::Info, "开始执行图: event-123");

// 节点执行
log_exec!(LogLevel::Debug, "执行节点: Print", "node-456");

// 执行完成
log_exec!(LogLevel::Info, "图执行完成");

// 执行错误
log_exec!(LogLevel::Error, "节点执行失败: 类型不匹配", "node-789");
```

#### 示例 3: 系统日志
```rust
// 系统信息
log_sys!(LogLevel::Info, "系统初始化完成");

// 系统警告
log_sys!(LogLevel::Warn, "内存使用率: 85%");

// 系统错误
log_sys!(LogLevel::Error, "磁盘空间不足");
```

### 前端显示

日志窗口会实时显示所有日志：
```
14:23:45.123  INFO   [APP]  [ProjectLoader] 正在加载项目
14:23:45.456  INFO   [EXEC] 开始执行图: event-123
14:23:45.789  DEBUG  [EXEC] [node-456] 执行节点: Print
14:23:46.012  INFO   [EXEC] 图执行完成
14:23:46.234  WARN   [SYS]  内存使用率: 85%
```

---

## 集成到现有代码

### 1. 在执行上下文中使用

修改 `src-tauri/src/executor/context.rs`:
```rust
use crate::logging::LogLevel;

impl ExecutionContext {
    pub fn log(&mut self, message: String) {
        // 同时记录到内部日志和发送到前端
        self.logs.push(message.clone());
        
        // 发送到前端日志窗口
        log_exec!(LogLevel::Info, message);
    }
}
```

### 2. 在命令中使用

修改 `src-tauri/src/commands/project.rs`:
```rust
use crate::logging::LogLevel;

#[tauri::command]
pub async fn load_project(path: String) -> Result<ProjectData, String> {
    log_app!(LogLevel::Info, format!("加载项目: {}", path), "ProjectCommand");
    
    match load_project_internal(&path) {
        Ok(data) => {
            log_app!(LogLevel::Info, "项目加载成功", "ProjectCommand");
            Ok(data)
        }
        Err(e) => {
            log_app!(LogLevel::Error, format!("项目加载失败: {}", e), "ProjectCommand");
            Err(e)
        }
    }
}
```

### 3. 在节点执行中使用

修改节点的执行逻辑：
```rust
use crate::logging::LogLevel;

node.set_flow_processor(Box::new(|ctx, node| {
    log_exec!(LogLevel::Debug, format!("执行节点: {}", node.name), &node.id);
    
    // 节点逻辑...
    
    log_exec!(LogLevel::Debug, format!("节点执行完成: {}", node.name), &node.id);
    Ok("Out".into())
}));
```

---

## 前端集成

### 1. 在布局中添加日志窗口

修改 `src/components/Editor/Layout/LayoutNodeRenderer.tsx`:
```typescript
import { LogWindow } from '../../LogView/LogWindow';

// 在视图注册中添加
const viewRegistry = {
  // ... 其他视图
  'log': LogWindow,
};
```

### 2. 添加日志窗口菜单项

在菜单或工具栏中添加打开日志窗口的按钮：
```typescript
<button onClick={() => openLogWindow()}>
  查看日志
</button>
```

### 3. 使用日志存储

在其他组件中访问日志：
```typescript
import { useLogStore } from '../../store/logStore';

function MyComponent() {
  const { logs, clearLogs } = useLogStore();
  
  return (
    <div>
      <p>日志数量: {logs.length}</p>
      <button onClick={clearLogs}>清空</button>
    </div>
  );
}
```

---

## 高级功能

### 1. 日志导出

添加导出日志功能：
```typescript
const exportLogs = () => {
  const logs = useLogStore.getState().logs;
  const content = logs.map(log => 
    `${log.timestamp} [${log.level.toUpperCase()}] [${log.log_type}] ${log.source ? `[${log.source}] ` : ''}${log.message}`
  ).join('\n');
  
  // 保存到文件
  const blob = new Blob([content], { type: 'text/plain' });
  const url = URL.createObjectURL(blob);
  const a = document.createElement('a');
  a.href = url;
  a.download = `logs-${Date.now()}.txt`;
  a.click();
};
```

### 2. 日志持久化

将日志保存到本地存储：
```typescript
// 保存日志
localStorage.setItem('logs', JSON.stringify(logs));

// 加载日志
const savedLogs = JSON.parse(localStorage.getItem('logs') || '[]');
```

### 3. 日志统计

显示日志统计信息：
```typescript
const stats = {
  total: logs.length,
  errors: logs.filter(l => l.level === 'error').length,
  warnings: logs.filter(l => l.level === 'warn').length,
  byType: {
    application: logs.filter(l => l.log_type === 'application').length,
    execution: logs.filter(l => l.log_type === 'execution').length,
    system: logs.filter(l => l.log_type === 'system').length,
  }
};
```

---

## 性能优化

### 1. 日志数量限制
- 最多保留 1000 条日志
- 超过限制时自动删除最旧的日志

### 2. 虚拟滚动
对于大量日志，可以使用虚拟滚动：
```typescript
import { FixedSizeList } from 'react-window';

<FixedSizeList
  height={600}
  itemCount={filteredLogs.length}
  itemSize={24}
  width="100%"
>
  {({ index, style }) => (
    <div style={style}>
      {/* 渲染日志行 */}
    </div>
  )}
</FixedSizeList>
```

### 3. 防抖搜索
搜索时使用防抖：
```typescript
const debouncedSearch = useMemo(
  () => debounce((text: string) => setSearchText(text), 300),
  []
);
```

---

## 编译状态
✅ **后端编译成功**
- 只有 9 个警告（与此实现无关）
- 无编译错误

✅ **前端组件已创建**
- 类型定义完成
- 日志存储完成
- 日志窗口组件完成

## 文件清单

### 后端
1. `src-tauri/src/logging.rs` - 日志管理器
2. `src-tauri/src/lib.rs` - 初始化日志管理器

### 前端
1. `src/types/logging.ts` - 类型定义
2. `src/store/logStore.ts` - 日志存储
3. `src/components/LogView/LogWindow.tsx` - 日志窗口组件

## 状态
✅ **实现完成，可以测试**

现在你可以在后端使用 `log_app!`, `log_exec!`, `log_sys!` 宏发送日志，前端会实时显示在日志窗口中！
